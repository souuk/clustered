% =================================================
% SHEEPLE TO DO BOARD AND NOTES
% =================================================
%
% Sheets 3 and 4 are one continuous instrument-design and hardware section.
% Sheet 5 explains the software architecture, and Sheet 6 explains the actual
% approach, feedback, and raster sequence.
%
% Sheet 7 now has the implemented file reader, schema check, conversion method,
% firmware-format synthetic data, and the raw-data-to-matrix figure.
%
% Sheet 8 now shows the tested synthetic four-panel Q output. LEAVE SHEET 8
% ALONE until we obtain a good, complete scan from our own STM. When that
% happens, replace the synthetic triplet, figure, caption, and summary together.
%
% LEAVE ALL OF SHEET 10 AND THE TOP HALF OF SHEET 11 ALONE until we have our
% own actual measurements. On Sheet 11, this freeze ends at the horizontal rule;
% the conclusion and Error Analysis and Future Work below it may still be edited.
%
% Sheet 9 contains two completed, clearly labeled simulations: the tight-binding
% eigenvalue/eigenvector calculation and the noise/leveling robustness sweep.
% Sheets 10 and 11 hold the remaining numbered TBM plots, then the conclusion
% and brief error analysis and future work.
%
% Main things still to do:
% - Get repeatable tunneling and one complete forward/backward scan of our own.
% - Measure the Z-piezo calibration before calling Q physical height.
% - Make remaining Figures #1, #2, #4, #5, and #7 with real measurements or
%   clearly labeled calculations. Figures #3 and #6 are now on Sheet 9.
% - Add final subsystem values, uncertainty, strongest result, and conclusion.
% - Verify the remaining hardware photos/labels and compile all 12 pages in
%   Overleaf to catch overflow.
% - Keep changes local for now. Do one GitHub push only when the group asks.
%
% =================================================
% FORMATTING NOTES
% =================================================
% Different main sections on the same page should have a 2 cm gap.
% Smaller subsections that are still part of the same topic get a 1 cm gap.
%
% The first paragraph after a heading should not be indented, but the
% paragraphs after it should be. Keep everything black and white except
% for real Matplotlib data graphs.
%
% Keep main.tex and all of its PNGs together in transform so we can
% upload the folder to Overleaf at once.
%
% For the full formatting and workflow list, check:
% https://github.com/souuk/clustered/blob/main/POSTER_FORMATTING_AND_WORKFLOW_RULES.txt
%
% =================================================
% POSTER LAYOUT MAP
% =================================================
% TOP
%   Title block will be created separately in Overleaf
%
% LEFT PANEL
%   1. Abstract + Introduction and Motivation
%      + Data Provenance and Visualization Notice
%   2. Theoretical and Technical Background
%   3. Instrument Design, Construction, and Hardware Architecture
%
% CENTER PANEL
%   The top-center row remains empty in the assembled poster.
%
%   LEFT CENTER COLUMN          RIGHT CENTER COLUMN
%   4. Hardware continuation    5. Control Software Architecture
%   6. Control Logic + Scan     7. Data Output + Python Conversion
%   8. Python Results (FROZEN)  9. Computational Experiments
%
% RIGHT PANEL
%   10. Supplementary Plots and Validation Figures (FROZEN)
%   11. Diagnostics (TOP HALF FROZEN), Conclusion, Error Analysis + Future Work
%   12. References and Acknowledgments, with GitHub QR code at top right

\documentclass[9pt,oneside]{report}

% =================================================
% PACKAGES
% =================================================
\usepackage[letterpaper,margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{float}
\usepackage{array}
\usepackage{setspace}
\usepackage{caption}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{microtype}
\usepackage{newtxtext}
\usepackage{newtxmath}
\usepackage{pdflscape}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{qrcode}

% =================================================
% COLOR AND TYPOGRAPHY
% =================================================
\setstretch{1.12}
\setlength{\parindent}{1.5em}
\setlength{\parskip}{0pt}
\setlength{\emergencystretch}{3em}
\raggedbottom
\renewcommand{\arraystretch}{1.12}

% Use this only between two completely different sections on the same sheet.
% Subsection spacing is already handled below.
\newcommand{\categorygap}{\par\vspace{2cm}}
\newcommand{\figuretbm}[1]{%
  \textsuperscript{\mbox{\tiny\bfseries FIGURE \##1 TBM}}%
}
% Keep this directly below every synthetic/calculated plot for now.
% Remove it only when that plot is replaced by verified experimental data.
\newcommand{\temporarysyntheticnotice}{%
  \par\vspace{1pt}%
  {\color{red}\tiny\bfseries
  TEMPORARY --- SYNTHETIC, NOT EXPERIMENTAL.}%
  \par
}

\setcounter{secnumdepth}{0}

\titleformat{\section}
  {\normalfont\Large\bfseries\centering}
  {}
  {0pt}
  {\MakeUppercase}

\titleformat{\subsection}
  {\normalfont\large\bfseries\raggedright}
  {}
  {0pt}
  {}

\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries\raggedright}
  {}
  {0pt}
  {}

\titlespacing*{\section}{0pt}{0pt}{14pt}
\titlespacing*{\subsection}{0pt}{1cm}{4pt}
\titlespacing*{\subsubsection}{0pt}{1cm}{3pt}

\captionsetup{
  font=small,
  labelfont=bf,
  justification=centering,
  skip=5pt
}

\lstset{
  language=C++,
  basicstyle=\ttfamily\scriptsize,
  frame=single,
  breaklines=true,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  aboveskip=4pt,
  belowskip=4pt
}

\pagestyle{empty}
\urlstyle{same}

% =================================================
% DOCUMENT
% =================================================
\begin{document}

% =================================================
% LEFT PANEL: THREE PORTRAIT SHEETS
% =================================================

% -------------------------------------------------
% SHEET 1 OF 12 -- ABSTRACT + INTRODUCTION/MOTIVATION
%                 + DATA PROVENANCE/VISUALIZATION NOTICE
% -------------------------------------------------
% These three sections are sharing one sheet on purpose. Keep the 2 cm gaps
% below so they still look like separate parts when we print everything.
\section{Abstract}

% Keep this as the short version of the whole project. Once the STM is fully
% tested, update the last paragraph with what it actually did.
This project aims to design, construct, and test a scanning tunneling
microscope (STM) capable of measuring the surface topography of a conductive
sample at the nanoscale. An STM operates by applying a voltage bias between a
sharp metallic tip and the sample. When the tip is brought within a few
angstroms of the surface, electrons quantum mechanically tunnel across the
gap, producing a current that varies exponentially with tip--sample
distance.\figuretbm{1}

The instrument combines a piezoelectric scanner, coarse-positioning stepper
motor, current preamplifier, analog-to-digital converter, dual-rail power
supply, and vibration-isolation system. The tunneling-current signal is
measured and used by a feedback system to adjust the vertical position of the
tip. By maintaining a nearly constant current while scanning laterally, the
system can relate the required tip motion to changes in surface height.

Subsystem testing is being used to evaluate the power supply, preamplifier,
and analog-to-digital conversion stages.\figuretbm{2} The final performance
of the STM is limited by mechanical vibration, electrical noise, thermal
drift, tip sharpness, and calibration accuracy. These tests establish the
operating principles of the instrument and identify the improvements needed
to achieve stable, repeatable nanoscale imaging.

\categorygap

\section{Introduction and Motivation}

% This part is more about why we made an STM, so do not repeat all the hardware
% details from the abstract here.
Scanning tunneling microscopy connects quantum mechanics to a practical
measurement technique. Because tunneling current is highly sensitive to
tip--sample separation, a feedback-controlled STM can detect very small
variations across a conductive surface. This project also integrates several
areas studied during COSMOS: wavefunctions and Hamiltonians, precision
current measurement, ADC and DAC conversion, PID feedback, piezoelectric
positioning, vibration isolation, and computational data collection.
Constructing an STM therefore provided both a nanoscale surface-measurement
goal and a practical demonstration of how quantum physics, electronics,
programming, and mechanical design operate
together.\textsuperscript{[1--3]}

\categorygap

\section{Data Provenance and Visualization Notice}

% Keep this notice even after the conversion code works. It makes it clear that
% the final map is processed data and not a photograph from the microscope.
% The converter and synthetic-data paths now exist in clustered. Add the exact
% group-owned raw-data path only after we obtain a reliable experimental scan.
The values recorded by the STM are the instrument's original measurement
output. Any surface image or three-dimensional rendering shown later is a
processed visualization produced by passing those measurements through a
separate modeling program; it is not a direct photograph from the STM. The
original STM data, the processed output, and the code used to convert the
measurements into a model will be added to the
\href{https://github.com/souuk/clustered}{clustered GitHub repository} as they
become available.

\clearpage

% -------------------------------------------------
% SHEET 2 OF 12 -- THEORETICAL AND TECHNICAL BACKGROUND
% -------------------------------------------------
\section{Theoretical and Technical Background}

% The spacing is tighter on this page because the formulas take up a lot of
% room. If this page ends up looking empty later, change 1.0 back to 1.12.
\begin{spacing}{1.0}
\setlength{\abovedisplayskip}{6pt}
\setlength{\belowdisplayskip}{6pt}

\subsection{Quantum-Mechanical Description}

% This is the quantum background from Chiang's notes. Keep the citation on both
% the explanation and the formula since both came from the notes.
Professor Chiang's notes describe an electron using a complex
wavefunction.\textsuperscript{[1]} For a model containing \(M\) possible
sites, the wavefunction is a vector with one component for each site. The
squared magnitude of a component gives the probability of finding the
electron at that location.\textsuperscript{[1]} A Hamiltonian matrix describes
the system's energy, and its eigenvalues and eigenvectors determine how the
state changes with time.\textsuperscript{[1]}\figuretbm{3}
\[
  P(j)=\lvert\psi_j\rvert^2,
  \qquad
  \psi(t)=e^{-iHt}\psi(0).
  \tag*{\text{\textsuperscript{[1]}}}
\]
This framework connects quantum probability to the vectors, matrices, and
time-evolution calculations studied in computational
physics.\textsuperscript{[1]} These eigenstate calculations are offline
computational models, not operations performed by the Teensy during STM
acquisition. Sheet~9 distinguishes the Hamiltonian matrix used in a quantum
model from the scan-data matrix produced by the
instrument.\textsuperscript{[1,2,4]}

\subsection{STM Signal Measurement}

% This is where the tiny current becomes a number the Teensy can actually use.
% If we measure a different resistor or ADC range later, update both the text
% and the formula together.
The supplied STM system converts a very small tunneling current into digital
data.\textsuperscript{[2]} Its preamplifier uses a
\(500\,\mathrm{M}\Omega\) feedback resistor and produces \(-0.5\) V for a
current of \(1\) nA.\textsuperscript{[2]} An 18-bit ADS8691
analog-to-digital converter maps voltages from \(-12.288\) to \(+12.288\) V
into integer values from \(0\) to \(262143\), with zero volts at
\(N_0=131072\).\textsuperscript{[2]}\figuretbm{4} The controller converts
each ADC reading \(N\) back into a voltage using
\[
  V_{\mathrm{ADC}}
  =\frac{(N-N_0)(12.288\ \mathrm{V})}{N_0}.
  \tag*{\text{\textsuperscript{[2]}}}
\]
The program also uses a four-channel 16-bit DAC to control the \(x\), \(y\),
and \(z\) piezo signals and the sample-bias voltage. Its default sample bias
is \(1.0\) V, represented by approximately \(2282\) DAC
counts.\textsuperscript{[2]}

\subsection{Approach, Feedback, and Topographic Mapping}

% This paragraph connects the physics to what the program actually does.
% Our measured approach threshold and final PID settings are still TBM. Replace
% the supplied defaults only after the final operating values are recorded.
During the automated approach, the program gradually extends the \(z\)-piezo
and repeatedly measures the current. A reading greater than \(0.2\) nA in
magnitude must be detected twice before the approach stops and feedback is
enabled.\textsuperscript{[2]} This repeated check helps prevent a single noisy
reading from being treated as a tunneling
signal.\textsuperscript{[2]}\figuretbm{5}

Once feedback is active, the included PID routine compares the measured ADC
value with the chosen current setpoint. It updates the \(z\)-piezo output
every \(100\,\mu\mathrm{s}\), using proportional, integral, and derivative
terms to reduce the error.\textsuperscript{[2,3]}\figuretbm{5} During imaging,
a second timer records points while the DAC moves through the \(x\) and \(y\)
coordinates.\textsuperscript{[2]} The program stores forward and backward scan
lines separately so the two directions can be
compared.\textsuperscript{[2]}

\end{spacing}

\clearpage

% -------------------------------------------------
% SHEET 3 OF 12 -- INSTRUMENT DESIGN, CONSTRUCTION,
%                  AND HARDWARE ARCHITECTURE
% -------------------------------------------------
\section{Instrument Design, Construction, and Hardware Architecture}

% Sheets 3 and 4 are one continuous section. This portrait sheet introduces
% the physical instrument and retains the selected materials list. The
% landscape continuation shows the electronic signal and control paths.
\begingroup
\titlespacing*{\subsubsection}{0pt}{0.45cm}{2pt}
\begin{spacing}{1.0}
\small

The instrument combines two scales of motion with a low-noise electronic
measurement chain. A stepper-driven mechanism first brings the tip into the
piezoelectric scanner's fine operating range. The suspended platform reduces
external vibration, while the electronics measure tunneling current and
command the scanner. The signal and control connections continue on
Sheet~4.

\vspace{4pt}

\noindent
\begin{minipage}[t]{0.48\textwidth}
  \vspace{0pt}
  \subsubsection{Dual-Rail Power Supply}

  The assembled supply provides \(+15\) V and \(-15\) V rails for the analog
  electronics. Components were soldered to the provided circuit board and
  installed in a 3D-printed enclosure. The outputs were checked relative to
  ground with a multimeter, and the trimmer potentiometers were adjusted to
  the target rail voltages.\figuretbm{2}

  \subsubsection{Coarse-Approach Mechanism}

  A geared stepper motor, metal plates, and machine screws provide the coarse
  motion needed to bring the tip close to the sample. The piezoelectric
  scanner is mounted at the center of this assembly and takes over once the
  remaining separation is within its fine-motion range.
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\textwidth}
  \vspace{0pt}
  \subsubsection{Piezoelectric Scanner}

  The scanner provides fine \(x\), \(y\), and \(z\) positioning. Lateral
  commands raster the tip across the sample, while the feedback-controlled
  vertical command responds to changes in the measured tunneling-current
  signal.

  \subsubsection{Vibration-Isolation System}

  Professor Shirley Chiang provided the spring-suspended platform used to
  isolate the microscope. Magnetic stabilizers were added at the base and
  sides to damp motion, and weights brought the suspended platform into the
  magnets' effective range. The completed approach assembly rests on this
  platform.
\end{minipage}

\subsection{Components and Materials}

% Keep this selected list unchanged until the group verifies the final bill of
% materials.
\noindent
\begin{minipage}[t]{0.57\textwidth}
  \vspace{0pt}
  \centering
  \scriptsize
  \setlength{\tabcolsep}{2pt}
  \begin{tabular}{>{\raggedright\arraybackslash}p{0.78\linewidth}
                  >{\raggedleft\arraybackslash}p{0.16\linewidth}}
    \toprule
    Component & Quantity \\
    \midrule
    Positive (LM317) and negative (LM337) adjustable voltage regulators & 1 each \\
    \(2200\,\mu\mathrm{F}\), 35 V capacitors & 2 \\
    \(1\,\mu\mathrm{F}\), 35 V tantalum capacitors & 2 \\
    General-purpose rectifier diodes & 4 \\
    \(1\,\mathrm{k}\Omega\) and \(180\,\Omega\) resistors & 1 and 2 \\
    LED uni-color red & 1\\
    Fast-acting glass fuse & 1\\
    \(2.5\,\mathrm{k}\Omega\) trimmer potentiometers & 2 \\
    12.6 V center-tapped transformer & 1 \\
    Custom PCB & 1\\
    TO-220 heat sinks & 2 \\
    Machine screws, lock washers, and hex nuts & As required \\
    \bottomrule
  \end{tabular}
  \captionsetup{font=scriptsize}
  \captionof{table}{Selected power-supply and mechanical components.}
\end{minipage}
\hfill
\begin{minipage}[t]{0.40\textwidth}
  \vspace{0pt}
  \centering
  \includegraphics[width=\linewidth]{dual-rail-psu.png}
  \captionsetup{font=scriptsize}
  \captionof{figure}{Dual-rail supply functional overview, simplified from the
  supplied JE215 handouts. Sheet~4 continues the electronic signal and control
  path.}
\end{minipage}

\end{spacing}
\endgroup

\clearpage

% =================================================
% CENTER PANEL: SIX LANDSCAPE SHEETS
% =================================================
% In the assembled center panel, Sheets 4, 6, and 8 form the hardware column.
% Sheets 5, 7, and 9 form the software/computation column.
% The row above these six sheets is intentionally left empty.

\begin{landscape}

% -------------------------------------------------
% CENTER ROW 1, HARDWARE COLUMN
% SHEET 4 OF 12 -- INSTRUMENT DESIGN, CONSTRUCTION,
%                  AND HARDWARE ARCHITECTURE, CONTINUED
% -------------------------------------------------
\section{Instrument Design, Construction, and Hardware Architecture
  (Continued)}

\begin{spacing}{0.98}
\small

Continuing directly from Sheet~3, the physical scanner, coarse-approach
assembly, isolation platform, and dual-rail supply connect to the electronic
measurement-and-control chain. Starting at the tip--sample junction, this path
is
sample and tip \(\longrightarrow\) preamplifier
\(\longrightarrow\) ADS8691 ADC
\(\longrightarrow\) Teensy 4.1
\(\longrightarrow\) DAC8734
\(\longrightarrow\) piezo drive amplifier
\(\longrightarrow\) scanner and sample bias. This completes the instrument
overview begun on Sheet~3. The diagram below expands the controller's
STM-specific interfaces, channel assignments, storage path, and separate power
domains.\textsuperscript{[2]}

\vspace{5pt}

\begin{center}
  \includegraphics[
    width=0.82\linewidth,
    height=3.85in,
    keepaspectratio
  ]{teensy-interface.png}
  \captionsetup{font=footnotesize}
  \captionof{figure}{Teensy 4.1 controller connections extracted from the
  supplied 2025 STM wiring list. Verify the diagram against the final assembled
  instrument before publication.}
\end{center}

\end{spacing}

\clearpage

% -------------------------------------------------
% CENTER ROW 1, SOFTWARE COLUMN
% SHEET 5 OF 12 -- CONTROL SOFTWARE ARCHITECTURE
% -------------------------------------------------
\section{Control Software Architecture}

% Keep this page at the big-picture level. The detailed decisions and loops go
% on Sheet 7 so we do not explain the same thing twice.
\begin{spacing}{1.0}
\small

The firmware is the coordination layer between the STM's measurement signal,
motion commands, feedback controller, user interface, and data storage. The
Teensy 4.1 acts as the central controller: it receives the digitized current
measurement, calculates or schedules the required response, sends commands to
the DAC channels, and writes the resulting scan values to the SD
card.\textsuperscript{[2,3]}

\vspace{4pt}

\noindent
\begin{minipage}[t]{0.43\linewidth}
  \vspace{0pt}

  \subsubsection{Central Controller}

  The Teensy communicates with the ADC and DAC over SPI, updates the LCD and
  serial interface, and manages the SD card. The ADC supplies the measured
  current signal. DAC channel~0 controls the vertical correction, channels~1
  and~2 control lateral motion, and channel~3 supplies the sample-bias
  command.\textsuperscript{[2]}

  \subsubsection{Program Organization}

  The main program initializes the interfaces, establishes safe starting
  outputs, interprets commands, and changes operating-state flags. Timer~1 is
  reserved for rapid current feedback, while Timer~2 advances the raster scan
  and records one value at each point. Separating these tasks allows feedback
  to remain active while scanning and storage continue.\textsuperscript{[2,3]}

  \subsubsection{Operating States}

  The firmware moves through initialization, approach, feedback, scan
  configuration, acquisition, and file-closing states. Each state enables only
  the actions required at that stage. The detailed decisions inside approach,
  feedback, and scanning are presented on the next software
  sheet.\textsuperscript{[2]}
\end{minipage}
\hfill
\begin{minipage}[t]{0.54\linewidth}
  \vspace{0pt}
  \centering
  % This is the main visual for the page. If the firmware organization changes,
  % update the PNG and the table below at the same time.
  \includegraphics[width=0.96\linewidth]{software-control-flow-large.png}
  \captionsetup{font=footnotesize}
  \captionof{figure}{High-level software architecture. Solid arrows show the
  feedback loop; dashed arrows show scan commands or stored
  data.\textsuperscript{[2,3]}}

  \vspace{4pt}

  % This table is the quick version for people who do not want to follow every
  % arrow in the diagram.
  \begin{tabular}{>{\raggedright\arraybackslash}p{0.29\linewidth}
                  >{\raggedright\arraybackslash}p{0.61\linewidth}}
    \toprule
    Software layer & Primary responsibility \\
    \midrule
    Main program & Initialization, commands, and state selection \\
    Timer 1 & Current measurement and vertical feedback \\
    Timer 2 & Raster coordinates and point storage \\
    File system & Forward, backward, and parameter files \\
    \bottomrule
  \end{tabular}
\end{minipage}

\end{spacing}

\clearpage

% -------------------------------------------------
% CENTER ROW 2, SOFTWARE COLUMN
% SHEET 6 OF 12 -- STM CONTROL LOGIC AND SCAN EXECUTION
% -------------------------------------------------
\section{STM Control Logic and Scan Execution}

% This page is about what the code does step by step, not how every file and
% function is arranged. Sheet 5 already handles the overall organization.
\begingroup
% This sheet has more code than the others, so keep its spacing a little tighter.
\titlespacing*{\subsubsection}{0pt}{0.42cm}{1.5pt}
\setlength{\abovedisplayskip}{3pt}
\setlength{\belowdisplayskip}{3pt}
\begin{spacing}{0.94}
\footnotesize

The firmware converts an STM measurement into a repeatable operating sequence:
approach the sample, confirm a tunneling signal, activate constant-current
feedback, execute the raster scan, and close the data files safely. Separate
state flags and timer interrupts ensure that each process runs only after its
required conditions have been satisfied.\textsuperscript{[2,3]}

\vspace{2pt}

\noindent
\begin{minipage}[t]{0.48\linewidth}
  \vspace{0pt}

  \subsubsection{Automated Approach and Signal Confirmation}

  Feedback is disabled during approach. The program gradually changes the
  vertical DAC output and reads the ADC after each change. When the signal
  exceeds the programmed positive or negative threshold, the ADC is read a
  second time. Only two consecutive threshold crossings are accepted as a
  tunneling signal. If no signal is confirmed during the fine sweep, the
  vertical output retracts, the coarse motor advances one step, and the
  routine tries again.\textsuperscript{[2]}

  \noindent{\tiny\textit{Code:}
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1119--1159.\textsuperscript{[2]}\par}

  \subsubsection{Timer 1: Constant-Current Feedback}

  % This is simplified PID math so visitors can understand the idea without
  % reading the full library. Our final measured Kp, Ki, and Kd are still TBM.
  After confirmation, Timer~1 executes every \(100\,\mu\mathrm{s}\). It reads
  the ADC, calculates a new PID output, and writes that correction to DAC
  channel~0.\textsuperscript{[2]} The supplied PID routine uses
  \[
    u_n=K_Pe_n+I_n-K_D(x_n-x_{n-1}),
  \]
  where \(e_n\) is the setpoint error and \(x_n-x_{n-1}\) is the recent change
  in measured input. The accumulated integral term and final output are
  clamped to the configured limits to prevent requests outside the DAC
  range.\textsuperscript{[3]}

  \noindent{\tiny\textit{Code:}
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1164--1175;\enspace \nolinkurl{PID_vInt.cpp}, lines
  48--78 and 134--148.\textsuperscript{[2,3]}\par}

  \subsubsection{Timer 2: Raster-Scan Execution}

  Timer~2 advances \(x\) by one DAC step and stores the current feedback output
  at each point. After the forward line reaches the selected number of points,
  the direction reverses and a backward line is collected. Completing both
  passes resets \(x\), increments \(y\), and starts the next line. When the
  selected number of lines is reached, the timer stops, writes the parameters,
  and closes all files.\textsuperscript{[2]}

  \noindent{\tiny\textit{Code:}
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1178--1239.\textsuperscript{[2]}\par}

  \vspace{3pt}

  \noindent\textbf{Equation Variable Legend}

  \vspace{2pt}

  \noindent
  {\tiny
  \renewcommand{\arraystretch}{1.0}
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.10\linewidth}
                  >{\raggedright\arraybackslash}p{0.35\linewidth}
                  >{\raggedright\arraybackslash}p{0.10\linewidth}
                  >{\raggedright\arraybackslash}p{0.35\linewidth}@{}}
    \toprule
    \textbf{Var.} & \textbf{Meaning} & \textbf{Var.} & \textbf{Meaning} \\
    \midrule
    \(u_n\) & PID output at update \(n\) & \(K_P\) & Proportional gain \\
    \(e_n\) & Setpoint error & \(I_n\) & Integral correction \\
    \(K_D\) & Derivative gain & \(x_n\) & Current ADC input \\
    \(x_{n-1}\) & Previous ADC input & \(n\) & Update number \\
    \bottomrule
  \end{tabular}
  }

  \vspace{3pt}

  \noindent\textbf{Timer 1 Source Excerpt}

  \begin{lstlisting}[
    language=C++,
    basicstyle=\ttfamily\tiny,
    numbers=left,
    numberstyle=\tiny,
    numbersep=4pt,
    firstnumber=1168,
    aboveskip=2pt,
    belowskip=1pt
  ]
Input = ADC_Convert();
myPID.Compute();
int Value = Output;
SetDAC(0, Value);
  \end{lstlisting}
  \noindent{\tiny Timer~1 reads the ADC, runs the PID, and updates DAC~0.
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1168--1171.\textsuperscript{[2]}\par}
\end{minipage}
\hfill
\begin{minipage}[t]{0.49\linewidth}
  \vspace{0pt}
  \centering
  % Keep this diagram simple because the pseudocode right below it gives the
  % extra detail. Update both if the retry path or timer order changes.
  \includegraphics[width=0.88\linewidth]{software-state-sequence.png}
  \captionsetup{font=scriptsize}
  \captionof{figure}{Operating states and the no-signal retry
  path.\textsuperscript{[2]}}

  \raggedright
  \subsubsection{Operating Sequence in Pseudocode}

  % This is not copied code. It is just the logic rewritten so people can read
  % it quickly. Add any group-specific shutdown or error-handling step after
  % final instrument operation is verified.
  The pseudocode summarizes the conditions that move the controller from one
  state to the next.

  \begin{lstlisting}[language={},basicstyle=\ttfamily\tiny]
INITIALIZE
  set interfaces and safe outputs
APPROACH
  sweep Z; read current
  confirmed twice -> start Timer 1
  no signal -> retract; motor step; retry
TIMER 1 -- every 100 us
  current <- ADC
  Z DAC <- limited PID correction
TIMER 2 -- each point
  step X; store correction
  forward done -> reverse X
  backward done -> reset X; step Y
  final line -> save; close files
  \end{lstlisting}

  \subsubsection{Raster State Update}

  \begin{lstlisting}[
    language=C++,
    basicstyle=\ttfamily\tiny,
    numbers=left,
    numberstyle=\tiny,
    numbersep=4pt,
    firstnumber=1201,
    aboveskip=2pt,
    belowskip=1pt
  ]
pointcounter++;
if (pointcounter >= numpoints) fscan = false;
if (pointcounter >= (2 * numpoints)) {
  fscan = true;
  pointcounter = 0;
  if (yscan) {
    yv += stepsize;
    SetDAC(2, yv);
  }
  if (xscan) xv = xvinit;
  \end{lstlisting}
  \noindent{\tiny After both \(x\) passes, the code resets the point state,
  increments \(y\), and returns \(x\) to its start.
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1201--1210.\textsuperscript{[2]}\par}

  \vspace{2pt}
  \noindent{\tiny\textit{Implementation check:} During the backward pass,
  \texttt{pointcounter} runs from \texttt{numpoints} through
  \texttt{2*numpoints-1}, while lines 1194--1198 use it directly as the second
  index of \texttt{DataArray1B}. The index should be made zero-based before
  relying on the default 512-point scan data.\textsuperscript{[2]}\par}

  \vspace{2pt}
  \noindent{\tiny\textit{Companion scan model:}
  \href{https://github.com/souuk/clustered/blob/main/animate_scan_pattern.py}
  {\nolinkurl{animate_scan_pattern.py}} reproduces the firmware's forward
  pass, backward pass, \(x\) reset, and \(y\) step using the cited timer
  sequence.\textsuperscript{[2,4]}\par}
\end{minipage}

\end{spacing}
\endgroup

\clearpage

% -------------------------------------------------
% CENTER ROW 3, DATA PROCESSING -- PART 1
% SHEET 7 OF 12 -- DATA OUTPUT AND PYTHON CONVERSION
% -------------------------------------------------
\section{Data Output and Python Conversion}

\begin{spacing}{0.90}
\footnotesize

\begin{center}
  \includegraphics[
    width=0.90\linewidth,
    height=0.82in,
    keepaspectratio
  ]{data-conversion-architecture.png}
  \captionsetup{font=scriptsize,skip=2pt}
  \captionof{figure}{Data-conversion architecture implemented by
  \nolinkurl{tunnelpy}: matched files are validated, decoded, reshaped into
  signed \(Q\) matrices, aligned, and rendered.\textsuperscript{[4]}}
\end{center}

\vspace{1pt}

\noindent
\begin{minipage}[t]{0.52\linewidth}
  \vspace{0pt}
  \subsubsection{What the Teensy Outputs}

  The firmware stores PID output \(Q\), also sent to the \(z\)-axis DAC, as
  two-byte \texttt{int16\_t} values in separate forward and backward files.
  These are signed correction counts, not ADC-current samples or physical
  height. A text file records dimensions, completion counters, bias, and
  current settings.\textsuperscript{[5]}

  \begin{lstlisting}[
    basicstyle=\ttfamily\tiny,
    aboveskip=2pt,
    belowskip=2pt
  ]
DataArray1F[linenumber][pointcounter] =
    (int16_t)Output;
dataFileF.write(
    &(DataArray1F[linenumber][pointcounter]), 2);

DataArray1B[linenumber][pointcounter-numpoints] =
    (int16_t)Output;
dataFileB.write(
    &(DataArray1B[linenumber][pointcounter-numpoints]), 2);
  \end{lstlisting}
  \noindent{\tiny\textit{Source:}
  \nolinkurl{Teensy_STM12_July_29_2026_v1.ino}, array declarations at lines
  178--180, data writes at lines 1509--1523, and parameter output at lines
  1583--1596.\textsuperscript{[5]}\par}

  \vspace{1pt}
  \begin{center}
    \includegraphics[
      width=0.90\linewidth,
      height=0.58in,
      keepaspectratio
    ]{file-schema-validation.png}\\[-2pt]
    {\tiny A pass establishes file-structure consistency only; it does not
    establish tunneling, calibration, or experimental validity.}
  \end{center}
\end{minipage}
\hfill
\begin{minipage}[t]{0.45\linewidth}
  \vspace{0pt}
  \begin{center}
    \includegraphics[
      width=0.90\linewidth,
      height=1.78in,
      keepaspectratio
    ]{raw-data-to-matrix.png}
    \captionsetup{font=scriptsize,skip=2pt}
    \captionof{figure}{Synthetic PID-output sequence decoded and reshaped into
    a \(64\times64\) matrix. It tests tilt, drift, feedback lag, noise, and
    corrugation; it is not STM data or calibrated height.\textsuperscript{[5]}}
    \temporarysyntheticnotice
  \end{center}
\end{minipage}

\vspace{2pt}

\noindent
\fbox{\parbox[c][1.10in][c]{0.95\linewidth}{
  \scriptsize
  \raggedright
  \textbf{Conversion Method.}
  The reader matches \texttt{F/B/P} files by prefix and scan number, obtains
  \(N_x,N_y\) from the parameter record, decodes each binary \texttt{.hex} file
  as little-endian signed \texttt{int16}, and requires \(N_xN_y\) samples
  (\(2N_xN_y\) bytes). Empty, odd, truncated, or unexpectedly appended files
  are rejected unless recovery is requested explicitly.\textsuperscript{[4,5]}
  It forms \(Q_F=\operatorname{reshape}(q_F,N_y,N_x)\) and
  \(Q_B=\operatorname{fliplr}[\operatorname{reshape}(q_B,N_y,N_x)]\), preserving
  both directions before calculating their mean and difference. Values remain
  uncalibrated PID-output counts; no \(Q_{\mathrm{ref}}\) subtraction or
  \(\alpha\) factor is applied.\textsuperscript{[4]}
}}

\end{spacing}

\clearpage

% -------------------------------------------------
% CENTER ROW 3, DATA PROCESSING -- PART 2
% SHEET 8 OF 12 -- PYTHON CONVERSION RESULTS
% FROZEN: do not revise this sheet again until our group obtains good,
% complete experimental data. Replace every synthetic value on this sheet
% together so the figure, caption, and summary always describe one scan.
% -------------------------------------------------
\section{Python Conversion Results and Generated Visualizations}

\begin{spacing}{0.90}
\footnotesize

After validating file lengths and reshaping the samples, the Python plotter
renders four printer-friendly heatmaps from one matched scan. Until real
tunneling data exist, the development output remains labeled
\textbf{SYNTHETIC -- NOT EXPERIMENTAL}.

\vspace{2pt}

\begin{center}
  \includegraphics[
    width=0.90\linewidth,
    height=3.72in,
    keepaspectratio
  ]{synthetic-q-four-panel.png}
  \captionsetup{font=scriptsize,skip=2pt}
  \captionof{figure}{Synthetic \nolinkurl{SYNF1/SYNB1/SYNP1} output showing
  forward \(Q\), aligned backward \(Q\), their mean, and their difference.
  Leveling is off; grayscale uses the 2nd--98th percentiles. This is a software
  test, not an STM result.}
  \temporarysyntheticnotice
\end{center}

\vspace{2pt}

\noindent
\fbox{\parbox[c][0.62in][c]{0.95\linewidth}{
  \scriptsize
  \textbf{Synthetic Pipeline Summary}\hfill
  \texttt{Input: little-endian int16 \(Q\)}\quad
  \texttt{Matrix: \(64\times64\)}\quad
  \texttt{8,192 bytes/direction}\\[2pt]
  Both files pass exact-size and completion checks. After alignment:
  correlation \(0.989\), mean absolute difference \(138\) counts, and range
  \(4954\)--\(9841\) counts. These values validate only the simulator and
  converter; physical-height calibration remains TBM.\figuretbm{7}
}}

\end{spacing}

\clearpage

% -------------------------------------------------
% CENTER ROW 3, RIGHT CENTER COLUMN
% SHEET 9 OF 12 -- COMPUTATIONAL EXPERIMENTS
% -------------------------------------------------
\section{Computational Experiments and Low-Level STM Models}

\begin{spacing}{0.88}
\footnotesize

Computational experiments operate at two distinct levels. An offline quantum
model predicts electron states and probability distributions, whereas the STM
controller converts measurements, updates feedback, advances the raster, and
stores \(Q\). The Hamiltonian \(H\) and measured scan matrix \(Q\) therefore
serve different purposes.

\vspace{2pt}

\begin{center}
  \fbox{\parbox[c][0.44in][c]{0.95\linewidth}{
    \centering
    \textbf{TWO COMPUTATIONAL PATHS}\\[2pt]
    Quantum model:
    \(H\rightarrow\{E_n,\phi_n\}\rightarrow
    |\phi_n(j)|^2\rightarrow\) predicted spatial behavior
    \quad\textbar\quad
    STM pipeline:
    ADC samples \(\rightarrow\) PID and raster logic
    \(\rightarrow Q_{j,i}\rightarrow\) validated heatmap
  }}
  \captionsetup{font=tiny,skip=2pt}
  \captionof{figure}{Conceptual separation between an offline quantum-state
  calculation and the instrument's measurement-and-control computation.}
\end{center}

\vspace{1pt}

\noindent
\begin{minipage}[t]{0.49\linewidth}
  \vspace{0pt}
  \subsection{Experiment 1: Hamiltonian Eigenstates}

  For a finite Hamiltonian, solve \(H\phi_n=E_n\phi_n\) and plot
  \(P_n(j)=|\phi_n(j)|^2\). Each \(E_n\) is an allowed model energy, while
  \(\phi_n\) describes its spatial amplitude. Changing coupling, site energy,
  or boundaries reveals how the spectrum and state localization
  respond.\textsuperscript{[1]}

  \begin{center}
    \includegraphics[width=0.88\linewidth]{eigenvalue-spectrum.png}
    \captionsetup{font=tiny,skip=2pt}
    \captionof{figure}{Figure \#3: calculated spectrum and selected-state
    probability for an \(11\times11\) tight-binding model with open boundaries
    and one central defect. It is not an STM measurement.}
    \temporarysyntheticnotice
  \end{center}
\end{minipage}
\hfill
\begin{minipage}[t]{0.49\linewidth}
  \vspace{0pt}
  \subsection{Experiment 2: Data-Pipeline Robustness}

  The Python pipeline checks the signed 16-bit contract, rejects incomplete
  data, reshapes samples, aligns backward rows, and plots heatmaps. Optional
  plane leveling fits and subtracts \(q(x,y)=ax+by+c\); it changes the
  presentation, not the raw files.\textsuperscript{[4]}

\vspace{1pt}

\scriptsize
\renewcommand{\arraystretch}{0.94}
\setlength{\tabcolsep}{2pt}
  \begin{tabular}{>{\raggedright\arraybackslash}p{0.34\linewidth}
                  >{\raggedright\arraybackslash}p{0.57\linewidth}}
    \toprule
    Computational check & Failure or artifact it targets \\
    \midrule
    Binary length and shape & Truncated or mismatched scan files \\
    Backward alignment & Reversed spatial orientation \\
    Forward-only mode & Unsafe or missing backward data \\
    Plane-leveling test & Residual planar background \\
    Synthetic lattice test & Loss of resolvable repeated features \\
    Plot creation tests & Broken rendering paths \\
    \bottomrule
  \end{tabular}

\vspace{2pt}
  \noindent{\tiny\textbf{Current local regression check:} six of six
  tests pass using the repository's declared plotting dependencies. These are
  software tests with synthetic fixtures, not evidence of tunneling or STM
  resolution.\textsuperscript{[4]}\par}

  \begin{center}
    \includegraphics[width=0.70\linewidth]{noise-leveling-sweep.png}
    \captionsetup{font=tiny,skip=2pt}
    \captionof{figure}{Figure \#6: fixed-seed synthetic noise and leveling
    test. Heatmaps compare no, line-median, and plane leveling; the curve
    reports recovery RMSE. Not experimental data.}
    \temporarysyntheticnotice
  \end{center}
\end{minipage}

\end{spacing}

\end{landscape}

% =================================================
% RIGHT PANEL: THREE PORTRAIT SHEETS
% =================================================

% -------------------------------------------------
% SHEET 10 OF 12 -- SUPPLEMENTARY PLOTS AND VALIDATION FIGURES
% FROZEN: do not edit this sheet until our group obtains the actual experimental
% data needed for its validation plots. Replace placeholders only with figures
% derived from our own recorded measurements and documented processing.
% -------------------------------------------------
\section{Supplementary Plots and Validation Figures}

\begin{spacing}{0.92}
\footnotesize

The placeholders on Sheets~9--11 correspond to the numbered
\textsuperscript{\mbox{\tiny\bfseries FIGURE \#n TBM}} markers in the main
text. Replace each box only with a measured, simulated, or calculated result
whose provenance and processing steps are stated explicitly.

\vspace{6pt}

\noindent
\begin{minipage}[t]{0.48\textwidth}
  \vspace{0pt}
  \begin{center}
    \fbox{\parbox[c][2.30in][c]{0.90\linewidth}{
      \centering
      \textbf{FIGURE \#1 TBM}\\[3pt]
      \textbf{TUNNELING CURRENT VS.\ TIP--SAMPLE DISTANCE}\\[7pt]
      \textbf{PLOT PLACEHOLDER}\\[8pt]
      Suggested axes: separation \(z\) and normalized tunneling current
      \(I/I_0\). Show the expected exponential trend and identify whether the
      curve is theoretical, simulated, or measured.
    }}
    \captionsetup{font=footnotesize}
    \captionof{figure}{Needed for the tunneling-distance statement in the
    abstract.}
  \end{center}
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\textwidth}
  \vspace{0pt}
  \begin{center}
    \fbox{\parbox[c][2.30in][c]{0.90\linewidth}{
      \centering
      \textbf{FIGURE \#4 TBM}\\[3pt]
      \textbf{ADC TRANSFER AND CURRENT-CONVERSION CURVE}\\[7pt]
      \textbf{PLOT PLACEHOLDER}\\[8pt]
      Plot applied or calculated input voltage against ADC counts. Mark
      \(N_0=131072\), the usable voltage range, and the preamplifier conversion
      between tunneling current and voltage.
    }}
    \captionsetup{font=footnotesize}
    \captionof{figure}{Needed for the ADC mapping described on Sheet~2.}
  \end{center}
\end{minipage}

\vspace{5pt}

\noindent
\begin{minipage}[t]{0.48\textwidth}
  \vspace{0pt}
  \begin{center}
    \begin{minipage}{0.80\linewidth}
      \centering
      \begin{minipage}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{scan-pattern-quad-1.png}\\[-1pt]
        {\tiny\bfseries 1. Forward \(+X\)}
      \end{minipage}
      \hfill
      \begin{minipage}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{scan-pattern-quad-2.png}\\[-1pt]
        {\tiny\bfseries 2. Backward \(-X\)}
      \end{minipage}

      \vspace{2pt}

      \begin{minipage}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{scan-pattern-quad-3.png}\\[-1pt]
        {\tiny\bfseries 3. X reset; Y step}
      \end{minipage}
      \hfill
      \begin{minipage}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{scan-pattern-quad-4.png}\\[-1pt]
        {\tiny\bfseries 4. Accumulated raster}
      \end{minipage}
    \end{minipage}

    \vspace{2pt}
    \begin{minipage}{0.90\linewidth}
      \textbf{Abridged Python scan model}
      \begin{lstlisting}[
        language=Python,
        basicstyle=\ttfamily\tiny,
        aboveskip=2pt,
        belowskip=2pt
      ]
for line in range(lines):
    for pointcounter in range(points):
        ...
        x += step_size
    for pointcounter in range(points, 2 * points):
        ...
        x -= step_size
    y += step_size
    x = initial_x
      \end{lstlisting}
      \scriptsize
      Source: \href{https://github.com/souuk/clustered/blob/main/animate_scan_pattern.py\#L83-L117}
      {\nolinkurl{animate_scan_pattern.py}, lines 83--117}. The corresponding
      Teensy behavior is in
      \nolinkurl{Teensy_STM12_July_29_2026_v1.ino}, lines 1504--1544.
    \end{minipage}
    \captionsetup{font=footnotesize}
    \captionof{figure}{Four static stages of the modeled bidirectional raster:
    forward \(+X\), backward \(-X\), X reset with Y increment, and accumulated
    scan progress.}
  \end{center}
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\textwidth}
  \vspace{0pt}
  \begin{center}
    \fbox{\parbox[c][2.30in][c]{0.90\linewidth}{
      \centering
      \textbf{FIGURE \#5 TBM}\\[3pt]
      \textbf{APPROACH THRESHOLD AND PID RESPONSE}\\[7pt]
      \textbf{PLOT PLACEHOLDER}\\[8pt]
      Show the two-sample tunneling-threshold confirmation, then plot PID
      setpoint, measured input, error, and vertical correction against time.
      Report \(K_P\), \(K_I\), \(K_D\), update interval, settling time,
      overshoot, and any output limiting used.
    }}
    \captionsetup{font=footnotesize}
    \captionof{figure}{Needed to support the feedback-performance discussion.}
  \end{center}
\end{minipage}

\end{spacing}

\clearpage

% -------------------------------------------------
% SHEET 11 OF 12 -- ADDITIONAL DIAGNOSTICS, CONCLUSION, ERROR, AND FUTURE WORK
% TOP HALF FROZEN: do not edit the two diagnostic placeholders or their captions
% until our group obtains actual subsystem and piezo-calibration measurements.
% The freeze ends at the horizontal rule below; the conclusion and brief error
% analysis/future-work section remain editable.
% -------------------------------------------------
\section{Additional Diagnostics and Project Conclusions}

\begin{spacing}{0.90}
\footnotesize

\noindent
\begin{minipage}[t]{0.48\textwidth}
  \vspace{0pt}
  \begin{center}
    \fbox{\parbox[c][1.82in][c]{0.90\linewidth}{
      \centering
      \textbf{FIGURE \#2 TBM}\\[3pt]
      \textbf{SUBSYSTEM VALIDATION SUMMARY}\\[6pt]
      \textbf{PLOT PLACEHOLDER}\\[6pt]
      Combine measured \(+15\) V and \(-15\) V rail stability with relevant
      preamplifier and ADC checks. Include expected values, measured values,
      uncertainty, and pass/fail criteria.
    }}
    \captionsetup{font=scriptsize}
    \captionof{figure}{Needed to support the subsystem-testing claims on
    Sheets~1 and~3.}
  \end{center}
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\textwidth}
  \vspace{0pt}
  \begin{center}
    \fbox{\parbox[c][1.82in][c]{0.90\linewidth}{
      \centering
      \textbf{FIGURE \#7 TBM}\\[3pt]
      \textbf{PIEZO DISPLACEMENT CALIBRATION}\\[6pt]
      \textbf{PLOT PLACEHOLDER}\\[6pt]
      Plot measured displacement against controller counts for the relevant
      axis. Fit the calibration factor, show uncertainty or hysteresis, and
      state the calibrated range.
    }}
    \captionsetup{font=scriptsize}
    \captionof{figure}{Needed before converting controller counts to physical
    height.}
  \end{center}
\end{minipage}

\vspace{4pt}
\hrule
\vspace{4pt}

% END OF FROZEN TOP HALF OF SHEET 11.

\subsection{Overarching Conclusion}

This project establishes an end-to-end framework for scanning tunneling
microscopy: coarse approach brings the sample toward the tip, piezoelectric
actuation supplies fine three-axis motion, the preamplifier and ADC measure the
low-current response, the Teensy executes feedback and bidirectional raster
logic, and the Python pipeline validates and visualizes the recorded output.
Treating these elements as one traceable measurement chain is a central result
of the project. It makes the origin, orientation, and limitations of every
plotted value explicit rather than treating the final heatmap as an isolated
image.

The completed work documents the instrument architecture, identifies the
firmware's signed 16-bit \(Q\)-output format, corrects the interpretation of
backward acquisition, and tests file validation, matrix reconstruction,
leveling, and plotting with clearly labeled synthetic fixtures. These
computational results demonstrate that the analysis workflow can reject
truncated or ambiguous files and preserve the distinction between raw
controller counts and calibrated physical height. They validate the software
path, but they do not establish tunneling or atomic resolution.

The remaining scientific milestone is a stable, group-owned experimental scan.
A defensible STM result will require repeatable tunneling, a complete
forward/backward file set, agreement between opposite scan directions,
calibrated lateral and vertical scales, and recognizable structure that
persists across repeated scans. Meeting those criteria would convert the
presently documented and tested framework into a quantitative surface
measurement.

\vspace{5pt}
\subsection{Error Analysis and Future Work}

\renewcommand{\arraystretch}{1.08}
\setlength{\tabcolsep}{3pt}
\scriptsize
\begin{tabular}{>{\raggedright\arraybackslash}p{0.21\linewidth}
                >{\raggedright\arraybackslash}p{0.34\linewidth}
                >{\raggedright\arraybackslash}p{0.37\linewidth}}
  \toprule
  Source & Likely effect & Measurement needed \\
  \midrule
  Mechanical vibration & Blurred or repeated spatial features &
    Isolation comparison or vibration spectrum \\
  Electrical noise & Fluctuating current and \(Q\) output &
    Baseline RMS and peak-to-peak noise \\
  Thermal drift and piezo creep & Image shear or positional offset &
    Drift rate and repeated-scan comparison \\
  Tip and tunneling stability & Double-tip artifacts or incomplete scans &
    Tip record, current trace, and scan repeatability \\
  Calibration and indexing & Incorrect scale or directional mismatch &
    Fitted scale factors and forward/backward regression tests \\
  \bottomrule
\end{tabular}

\vspace{4pt}
\noindent\footnotesize
Future work should first stabilize tunneling and record one complete scan from
our instrument, then quantify the tabled contributions, calibrate all plotted
axes, and repeat the scan in both directions. Until those measurements exist,
the uncertainty assessment must remain qualitative; no error bars, calibration
factors, or successful imaging claims should be inferred from the synthetic
tests.

\end{spacing}

\clearpage

% -------------------------------------------------
% SHEET 12 OF 12 -- REFERENCES AND ACKNOWLEDGMENTS
% -------------------------------------------------
\section{References and Acknowledgments}

% If we add a new source anywhere else, add it here and make sure the little
% citation number in the text matches this list.
\noindent
\begin{minipage}[t]{0.72\textwidth}
  \vspace{0pt}
  \subsection{References}

  \begin{enumerate}
    \item S. Chiang, ``Additional Quantum Mechanics Discussion,''
    \textit{QMnotes1.pdf}, COSMOS course handout, 2026.

    \item S. Chiang, \textit{Teensy STM11A}, STM controller source file
    \nolinkurl{Teensy_STM11A_July_29_2025.ino}, University of California,
    Davis, 2025. Cited portions include lines 155--157, 838--857,
    1119--1239, and 1248--1262.

    \item B. Beauregard, \textit{Arduino PID Library}, version 1.1.1,
    timer-interrupt modification by S. Chiang, source file
    \nolinkurl{PID_vInt.cpp}, course software archive. Cited portions include
    lines 48--78 and 134--148.

    \item souuk, \textit{clustered: STM Project Files and Citation-Source
    Paths}, GitHub repository, 2026. Available:
    \href{https://github.com/souuk/clustered}{github.com/souuk/clustered}.
    The repository includes \nolinkurl{animate_scan_pattern.py} and four static
    poster frames named \nolinkurl{scan-pattern-quad-1.png} through
    \nolinkurl{scan-pattern-quad-4.png}; source locations within the supplied
    course archive are listed in the repository's
    \href{https://github.com/souuk/clustered\#citation-path-index}
    {citation path index}.

    \item S. Chiang, \textit{Teensy STM12}, STM controller source file
    \nolinkurl{Teensy_STM12_July_29_2026_v1.ino}, University of California,
    Davis, 2026. Cited portions include lines 178--180, 989--1001,
    1113--1131, 1500--1544, and 1583--1596.
  \end{enumerate}
\end{minipage}
\hfill
\begin{minipage}[t]{0.23\textwidth}
  \vspace{0pt}
  \raggedleft
  % This makes the QR code straight from the link, so we do not need to upload
  % another PNG for it in Overleaf.
  \href{https://github.com/souuk/clustered}{%
    \qrcode[height=1.25in]{https://github.com/souuk/clustered}%
  }

  \vspace{5pt}
  {\small\bfseries STM source code\par}
  {\footnotesize github.com/souuk/clustered\par}
\end{minipage}

\subsection{Acknowledgments}

We are deeply grateful to the instructors and mentors whose guidance,
encouragement, patience, and generosity made this project possible.

We thank Professor Shirley Chiang for guiding the experimental side of the
project. Her instruction in E\&M, STM operation, and circuitry gave us the
practical foundation needed to construct and test the microscope. We are
especially grateful for the insight she provided throughout the work.

We also thank Professor Richard Scalettar for providing the computational labs
that supported this project. These labs allowed us to model quantum systems
and simulate physical behavior that we could not directly reproduce with our
experimental setups. We also thank him for all the amazing free clothes; they
will decorate our souls for years to come.

Finally, we thank Teaching Intern ``Multimeter Whisperer'' Gibson Meeker.
Although we sometimes joked that his official role was to ``babysit us,'' his
constant advice, patience, and good humor made every stage of the project
smoother and far more enjoyable. His steady presence and, of course, his
invention of the ``Gibson Tech'' were invaluable parts of our COSMOS
experience.

\end{document}
