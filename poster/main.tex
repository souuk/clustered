% =================================================
% SHEEPLE TO DO BOARD AND NOTES
% =================================================
% Justin: content sharing one printed page can still use separate sections.
%
% Page 6 now documents software verification and data integrity.
% Keep its firmware line references synchronized with the supplied source and
% do not describe the backward-array indexing defect as fixed until the
% Teensy code itself has been corrected and tested.
%
% Someone needs to check the components and materials table against
% what we actually used. Do not leave guessed quantities in there.
%
% We need good pictures of the power supply, piezo and tip, motor
% mechanism, vibration setup, and the finished STM. Label them if it
% is not obvious what people are looking at.
%
% We should make one simple hardware diagram if the hardware page
% still has a bunch of empty space after the writing is finished.
%
% We need the actual multimeter values for both power rails and the
% actual function-generator and oscilloscope settings for the analog tests.
%
% We need the ADC test values, converted voltages, differences, and a
% graph if we took enough points. We also need the requested DAC values
% and the waveform or measured voltage that came out.
%
% Put all of those hardware tests into one results table with the expected
% value, measured value, units, difference, and whether the part passed.
%
% We need to record the final approach threshold and whatever Kp, Ki,
% and Kd values we actually use. Do not leave the supplied defaults there
% if we end up changing them.
%
% The Timer 2 backward-array index has been checked and is unsafe at the
% default size: pointcounter begins the backward pass at 512, but each
% DataArray1B row is only 512 elements wide. Correct and test the firmware
% before trusting backward files.
%
% The executable file writes use signed int16_t values and two bytes per
% sample, although the adjacent source comment says 18 bits and four bytes.
% Resolve that documentation conflict and define one explicit file schema.
%
% For data output, we still need a complete forward file, backward file,
% and parameter file from a real scan. A Serial Monitor picture is not
% the same thing as the scan data.
%
% We need to finish and test the Python reconstruction code on those real
% files. It should check the file length, read the scan dimensions, reshape
% the values, reverse the backward direction, and keep the raw matrix.
%
% We need to make sure x and y are facing the right way and compare the
% forward and backward scans before combining anything.
%
% We cannot call the vertical values actual height until we measure the
% z-piezo calibration. Until then, call them relative correction or counts.
%
% Once the code works, fill in the blank scan table and replace all three
% "Add about" sentences on Page 9 with the real settings, code, and results.
%
% Make the final 2D Matplotlib surface map from the real data. A raised
% 3D view is optional and should be called a visualization of the same
% 2D matrix, not a separate three-dimensional measurement.
%
% We need to write the results and uncertainty page after the final tests.
% Only say what the data actually supports, then connect each improvement
% to a problem we really observed.
%
% We need to decide which extra graphs or test pictures actually belong
% on the supplementary figures page. Do not add filler just to use space.
%
% Update the abstract after the final scan and tests so it does not claim
% more than the finished STM actually did.
%
% Add the exact raw-data and processing-code paths to the data notice once
% they are in clustered. Put the real files, code, settings, calibration,
% and final figures in the repo together.
%
% Check every code citation and line number after the writing is final.
% If we add another source, update Works Cited and the citation-path file too.
%
% Finish the title block in Overleaf with the final title, names, program,
% and whatever other information the group decides belongs there.
%
% Add anyone else who helped us to the acknowledgments before printing.
%
% Compile the whole folder in Overleaf and look at every page for overflow.
% Check the 2 cm and 1 cm gaps, paragraph indents, table widths, code boxes,
% captions, image quality, and whether the font is readable when printed.
%
% Make sure every PNG used by main.tex is uploaded beside it, test the GitHub
% QR code, and print one black-and-white test page before doing the full poster.
%
% We should decide who is handling each unfinished page so two people do not
% work on the same thing while another page gets forgotten.
%
% Save the final scan settings in one place so the numbers on the poster,
% the parameter file, and the reconstruction code all match.
%
% Keep one untouched copy of the raw scan files before we start processing
% them, and put each usable scan in its own clearly named folder.
%
% Check the software diagrams against the final firmware so the arrows,
% timer steps, and file-writing order are still correct.
%
% Remove every visible placeholder, blank line, and "for future" note before
% the final PDF is sent for printing.
%
% Save one clearly named final PDF and a copy of the matching Overleaf folder
% so everyone knows which version is actually ready to print.
% =================================================
% FORMATTING NOTES
% =================================================
% Different main sections on the same page should have a 2 cm gap.
% Smaller subsections that are still part of the same topic get a 1 cm gap.
%
% The first paragraph after a heading should not be indented, but the
% paragraphs after it should be. Keep everything black and white except
% for real Matplotlib data graphs.
%
% Keep main.tex and all of its PNGs together in transform so we can
% upload the folder to Overleaf at once.
%
% For the full formatting and workflow list, check:
% https://github.com/souuk/clustered/blob/main/POSTER_FORMATTING_AND_WORKFLOW_RULES.txt
%
% =================================================
% POSTER LAYOUT MAP
% =================================================
% TOP
%   Title block will be created separately in Overleaf
%
% LEFT PANEL
%   1. Abstract + Introduction and Motivation
%      + Data Provenance and Visualization Notice
%   2. Theoretical and Technical Background
%   3. Instrument Design and Construction
%
% CENTER PANEL
%   The top-center row remains empty in the assembled poster.
%
%   HARDWARE/VALIDATION COLUMN  SOFTWARE COLUMN
%   4. Hardware Architecture    5. Control Software Architecture
%   6. Software Verification    7. Control Logic and Scan Execution
%   8. Testing and Calibration  9. Data Output and Surface Reconstruction
%
% RIGHT PANEL
%   10. Results and Uncertainty Analysis
%   11. Supplementary Figures
%   12. References and Acknowledgments, with GitHub QR code at top right

\documentclass[9pt,oneside]{report}

% =================================================
% PACKAGES
% =================================================
\usepackage[letterpaper,margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{float}
\usepackage{array}
\usepackage{setspace}
\usepackage{caption}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{microtype}
\usepackage{newtxtext}
\usepackage{newtxmath}
\usepackage{pdflscape}
\usepackage[hidelinks]{hyperref}
\usepackage{qrcode}

% =================================================
% COLOR AND TYPOGRAPHY
% =================================================
\setstretch{1.12}
\setlength{\parindent}{1.5em}
\setlength{\parskip}{0pt}
\setlength{\emergencystretch}{3em}
\raggedbottom
\renewcommand{\arraystretch}{1.12}

% Use this only between two completely different sections on the same sheet.
% Subsection spacing is already handled below.
\newcommand{\categorygap}{\par\vspace{2cm}}

\setcounter{secnumdepth}{0}

\titleformat{\section}
  {\normalfont\Large\bfseries\centering}
  {}
  {0pt}
  {\MakeUppercase}

\titleformat{\subsection}
  {\normalfont\large\bfseries\raggedright}
  {}
  {0pt}
  {}

\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries\raggedright}
  {}
  {0pt}
  {}

\titlespacing*{\section}{0pt}{0pt}{14pt}
\titlespacing*{\subsection}{0pt}{1cm}{4pt}
\titlespacing*{\subsubsection}{0pt}{1cm}{3pt}

\captionsetup{
  font=small,
  labelfont=bf,
  justification=centering,
  skip=5pt
}

\lstset{
  language=C++,
  basicstyle=\ttfamily\scriptsize,
  frame=single,
  breaklines=true,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  aboveskip=4pt,
  belowskip=4pt
}

\pagestyle{empty}
\urlstyle{same}

% =================================================
% DOCUMENT
% =================================================
\begin{document}

% =================================================
% LEFT PANEL: THREE PORTRAIT SHEETS
% =================================================

% -------------------------------------------------
% SHEET 1 OF 12 -- ABSTRACT + INTRODUCTION/MOTIVATION
%                 + DATA PROVENANCE/VISUALIZATION NOTICE
% -------------------------------------------------
% These three sections are sharing one sheet on purpose. Keep the 2 cm gaps
% below so they still look like separate parts when we print everything.
\section{Abstract}

% Keep this as the short version of the whole project. Once the STM is fully
% tested, update the last paragraph with what it actually did.
This project aims to design, construct, and test a scanning tunneling
microscope (STM) capable of measuring the surface topography of a conductive
sample at the nanoscale. An STM operates by applying a voltage bias between a
sharp metallic tip and the sample. When the tip is brought within a few
angstroms of the surface, electrons quantum mechanically tunnel across the
gap, producing a current that varies exponentially with tip--sample distance.

The instrument combines a piezoelectric scanner, coarse-positioning stepper
motor, current preamplifier, analog-to-digital converter, dual-rail power
supply, and vibration-isolation system. The tunneling-current signal is
measured and used by a feedback system to adjust the vertical position of the
tip. By maintaining a nearly constant current while scanning laterally, the
system can relate the required tip motion to changes in surface height.

The firmware's control flow and raster order have been traced to the supplied
source and reproduced in a separate Python model. Quantitative power-supply,
signal-chain, ADC, DAC, and complete-scan measurements have not yet been
recorded in the project archive. The present work therefore establishes the
instrument design, software behavior, and remaining validation requirements;
it does not yet demonstrate calibrated nanoscale surface imaging.

\categorygap

\section{Introduction and Motivation}

% This part is more about why we made an STM, so do not repeat all the hardware
% details from the abstract here.
Scanning tunneling microscopy connects quantum mechanics to a practical
measurement technique. Because tunneling current is highly sensitive to
tip--sample separation, a feedback-controlled STM can detect very small
variations across a conductive surface. This project also integrates several
areas studied during COSMOS: wavefunctions and Hamiltonians, precision
current measurement, ADC and DAC conversion, PID feedback, piezoelectric
positioning, vibration isolation, and computational data collection.
Constructing an STM therefore provided both a nanoscale surface-measurement
goal and a practical demonstration of how quantum physics, electronics,
programming, and mechanical design operate
together.\textsuperscript{[1--3]}

\categorygap

\section{Data Provenance and Visualization Notice}

% Keep this notice even after the conversion code works. It makes it clear that
% the final map is processed data and not a photograph from the microscope.
% Insert the exact raw-data and code paths here for future once they are in
% clustered.
The values recorded by the STM are the instrument's original measurement
output. Any surface image or three-dimensional rendering shown later is a
processed visualization produced by passing those measurements through a
separate modeling program; it is not a direct photograph from the STM. The
scan-order verification code is available in the
\href{https://github.com/souuk/clustered}{clustered GitHub repository} as
\nolinkurl{animate_scan_pattern.py}, with its rendered preview stored as
\nolinkurl{scan-pattern-preview.gif}. Complete raw STM measurements and a
tested surface-reconstruction program have not yet been added.

\clearpage

% -------------------------------------------------
% SHEET 2 OF 12 -- THEORETICAL AND TECHNICAL BACKGROUND
% -------------------------------------------------
\section{Theoretical and Technical Background}

% The spacing is tighter on this page because the formulas take up a lot of
% room. If this page ends up looking empty later, change 1.0 back to 1.12.
\begin{spacing}{1.0}
\setlength{\abovedisplayskip}{6pt}
\setlength{\belowdisplayskip}{6pt}

\subsection{Quantum-Mechanical Description}

% This is the quantum background from Chiang's notes. Keep the citation on both
% the explanation and the formula since both came from the notes.
Professor Chiang's notes describe an electron using a complex
wavefunction.\textsuperscript{[1]} For a model containing \(M\) possible
sites, the wavefunction is a vector with one component for each site. The
squared magnitude of a component gives the probability of finding the
electron at that location.\textsuperscript{[1]} A Hamiltonian matrix describes
the system's energy, and its eigenvalues and eigenvectors determine how the
state changes with time.\textsuperscript{[1]}
\[
  P(j)=\lvert\psi_j\rvert^2,
  \qquad
  \psi(t)=e^{-iHt}\psi(0).
  \tag*{\text{\textsuperscript{[1]}}}
\]
This framework connects quantum probability to the vectors, matrices, and
time-evolution calculations studied in computational
physics.\textsuperscript{[1]}

\subsection{STM Signal Measurement}

% This is where the tiny current becomes a number the Teensy can actually use.
% If we measure a different resistor or ADC range later, update both the text
% and the formula together.
The supplied STM system converts a very small tunneling current into digital
data.\textsuperscript{[2]} Its preamplifier uses a
\(500\,\mathrm{M}\Omega\) feedback resistor and produces \(-0.5\) V for a
current of \(1\) nA.\textsuperscript{[2]} An 18-bit ADS8691
analog-to-digital converter maps voltages from \(-12.288\) to \(+12.288\) V
into integer values from \(0\) to \(262143\), with zero volts at
\(N_0=131072\).\textsuperscript{[2]} The controller converts each ADC reading
\(N\) back into a voltage using
\[
  V_{\mathrm{ADC}}
  =\frac{(N-N_0)(12.288\ \mathrm{V})}{N_0}.
  \tag*{\text{\textsuperscript{[2]}}}
\]
The program also uses a four-channel 16-bit DAC to control the \(x\), \(y\),
and \(z\) piezo signals and the sample-bias voltage. Its default sample bias
is \(1.0\) V, represented by approximately \(2282\) DAC
counts.\textsuperscript{[2]}

\subsection{Approach, Feedback, and Topographic Mapping}

% This paragraph connects the physics to what the program actually does.
% Insert our final approach threshold and PID settings here for future if they
% end up being different from the supplied defaults.
During the automated approach, the program gradually extends the \(z\)-piezo
and repeatedly measures the current. A reading greater than \(0.2\) nA in
magnitude must be detected twice before the approach stops and feedback is
enabled.\textsuperscript{[2]} This repeated check helps prevent a single noisy
reading from being treated as a tunneling signal.\textsuperscript{[2]}

Once feedback is active, the included PID routine compares the measured ADC
value with the chosen current setpoint. It updates the \(z\)-piezo output
every \(100\,\mu\mathrm{s}\), using proportional, integral, and derivative
terms to reduce the error.\textsuperscript{[2,3]} During imaging, a second
timer records points while the DAC moves through the \(x\) and \(y\)
coordinates.\textsuperscript{[2]} The program stores forward and backward
scan lines separately so the two directions can be
compared.\textsuperscript{[2]}

\end{spacing}

\clearpage

% -------------------------------------------------
% SHEET 3 OF 12 -- INSTRUMENT DESIGN AND CONSTRUCTION
% -------------------------------------------------
\section{Instrument Design and Construction}

% Talk about what we physically built and how we put it together here.
% Save the testing stuff for Sheet 8.

\subsection{Physical Construction Scope}

The physical build consists of the dual-rail supply, piezoelectric scanner,
coarse-approach mechanism, tip and sample mount, and vibration-isolation
platform. Routine wiring is omitted here so the poster can emphasize the
signal path and control software. A complete bill of materials should be
restored only after every component and quantity has been checked against the
finished instrument.

\subsubsection{Dual-Rail Power Supply}

% Insert a clear photo of the finished power-supply board here for future.
% Try to use one where the wiring and the +15 V / -15 V labels are readable.
The power supply provides stable \(+15\) V and \(-15\) V rails. It was
constructed from a provided circuit board, resistors, capacitors,
transformers, regulators, and related components. The components were
soldered to the board, installed in a 3D-printed enclosure, and checked with
a multimeter. Trimmer potentiometers were adjusted until both output rails
reached their target voltages relative to ground.

\subsubsection{Piezoelectric Scanner}

% Insert a labeled close-up of the piezo and tip here for future if we have one.
The piezoelectric scanner provides fine \(x\), \(y\), and \(z\) positioning
of the tip. A voltage bias between the metallic tip and conductive sample
allows a tunneling current to form when their separation becomes sufficiently
small. During scanning, the \(x\)- and \(y\)-piezo signals move the tip
laterally while feedback changes the \(z\)-piezo command to keep the measured
current near its setpoint.

\subsubsection{Coarse-Approach Mechanism}

% Insert a photo showing the motor, plates, and direction of movement here for
% future. A side view would probably explain this better than a top view.
The stepper-motor mechanism provides coarse positioning so the tip can be
brought into the operating range of the piezoelectric scanner. The mechanism
was assembled from metal plates, screws, and a geared stepper motor. The
piezoelectric scanner was mounted at the center of the mechanism. The motor
closes most of the initial gap; the \(z\)-piezo then supplies the much smaller
fine-positioning corrections used during approach and imaging.

\subsubsection{Vibration-Isolation System}

% Insert a full photo of the suspended platform here for future. Label the
% springs, magnets, and added weights if the picture is not obvious by itself.
The spring-suspension system isolates the microscope from external
vibrations. Professor Shirley Chiang provided the suspended platform.
Magnetic stabilizers were added at the base and sides to damp motion, and
weights were added to bring the suspended platform into the effective range
of the magnets. The stepper-motor assembly was then placed on the platform.

\clearpage

% =================================================
% CENTER PANEL: SIX LANDSCAPE SHEETS
% =================================================
% In the assembled center panel, Sheets 4, 6, and 8 form the hardware column.
% Sheets 5, 7, and 9 form the software/computation column.
% The row above these six sheets is intentionally left empty.

\begin{landscape}

% -------------------------------------------------
% CENTER ROW 1, HARDWARE COLUMN
% SHEET 4 OF 12 -- HARDWARE ARCHITECTURE
% -------------------------------------------------
\section{Hardware Architecture}

% Explain what each electronic part does here. Keep the building details
% on Sheet 3 and the measurements on Sheet 8.
\begin{spacing}{1.0}
\small

\begin{figure}[H]
  \centering
  \includegraphics[width=0.90\linewidth]{hardware-signal-chain.png}
  \captionsetup{font=footnotesize}
  \caption{Critical measurement and control paths. Routine wiring is omitted;
  the diagram follows the preamplifier, ADC, DAC, and channel assignments
  documented by the controller source.\textsuperscript{[2]}}
\end{figure}

\noindent
\begin{minipage}[t]{0.48\linewidth}
  \vspace{0pt}
  \subsubsection{Tunneling-Current Preamplifier}

The current preamplifier converts the extremely small tunneling current
(typically picoamperes to nanoamperes) into a measurable voltage using a
transimpedance configuration. Its large feedback resistance provides the gain
needed for the ADC and feedback controller to detect nanoampere-scale changes.

  \subsubsection{Analog-to-Digital Converter}

The ADS8691 analog-to-digital converter digitizes the preamplifier voltage
with 18-bit resolution. The Teensy reads this value over SPI and supplies it
to the feedback controller as the measured tunneling-current signal.
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\linewidth}
  \vspace{0pt}
  \subsubsection{Digital-to-Analog Converter}

The four-channel 16-bit DAC8734 converts Teensy commands into analog outputs:
channel~0 supplies vertical correction, channels~1 and~2 control lateral
motion, and channel~3 supplies sample bias.\textsuperscript{[2]}

  \subsubsection{Piezoelectric Drive Amplifier}

The drive amplifier accepts the \(x\), \(y\), and \(z\) DAC commands and
produces the higher-voltage outputs required by the scanner. Its summing and
subtracting stages bend the tube laterally or extend it vertically.
\end{minipage}

\end{spacing}

\clearpage

% -------------------------------------------------
% CENTER ROW 1, SOFTWARE COLUMN
% SHEET 5 OF 12 -- CONTROL SOFTWARE ARCHITECTURE
% -------------------------------------------------
\section{Control Software Architecture}

% Keep this page at the big-picture level. The detailed decisions and loops go
% on Sheet 7 so we do not explain the same thing twice.
\begin{spacing}{1.0}
\small

The firmware is the coordination layer between the STM's measurement signal,
motion commands, feedback controller, user interface, and data storage. The
Teensy 4.1 acts as the central controller: it receives the digitized current
measurement, calculates or schedules the required response, sends commands to
the DAC channels, and writes the resulting scan values to the SD
card.\textsuperscript{[2,3]}

\vspace{4pt}

\noindent
\begin{minipage}[t]{0.43\linewidth}
  \vspace{0pt}

  \subsubsection{Central Controller}

  The Teensy communicates with the ADC and DAC over SPI, updates the LCD and
  serial interface, and manages the SD card. The ADC supplies the measured
  current signal. DAC channel~0 controls the vertical correction, channels~1
  and~2 control lateral motion, and channel~3 supplies the sample-bias
  command.\textsuperscript{[2]}

  \subsubsection{Program Organization}

  The main program initializes the interfaces, establishes safe starting
  outputs, interprets commands, and changes operating-state flags. Timer~1 is
  reserved for rapid current feedback, while Timer~2 advances the raster scan
  and records one value at each point. Separating these tasks allows feedback
  to remain active while scanning and storage continue.\textsuperscript{[2,3]}

  \subsubsection{Operating States}

  The firmware moves through initialization, approach, feedback, scan
  configuration, acquisition, and file-closing states. Each state enables only
  the actions required at that stage. The detailed decisions inside approach,
  feedback, and scanning are presented on the next software
  sheet.\textsuperscript{[2]}
\end{minipage}
\hfill
\begin{minipage}[t]{0.54\linewidth}
  \vspace{0pt}
  \centering
  % This is the main visual for the page. If the firmware organization changes,
  % update the PNG and the table below at the same time.
  \includegraphics[width=0.96\linewidth]{software-control-flow-large.png}
  \captionsetup{font=footnotesize}
  \captionof{figure}{High-level software architecture. Solid arrows show the
  feedback loop; dashed arrows show scan commands or stored
  data.\textsuperscript{[2,3]}}

  \vspace{4pt}

  % This table is the quick version for people who do not want to follow every
  % arrow in the diagram.
  \begin{tabular}{>{\raggedright\arraybackslash}p{0.29\linewidth}
                  >{\raggedright\arraybackslash}p{0.61\linewidth}}
    \toprule
    Software layer & Primary responsibility \\
    \midrule
    Main program & Initialization, commands, and state selection \\
    Timer 1 & Current measurement and vertical feedback \\
    Timer 2 & Raster coordinates and point storage \\
    File system & Forward, backward, and parameter files \\
    \bottomrule
  \end{tabular}
\end{minipage}

\end{spacing}

\clearpage

% -------------------------------------------------
% CENTER ROW 2, VALIDATION COLUMN
% SHEET 6 OF 12 -- SOFTWARE VERIFICATION AND DATA INTEGRITY
% -------------------------------------------------
\section{Software Verification and Data Integrity}

\begin{spacing}{0.94}
\footnotesize

The Python model was derived from the supplied Teensy source rather than from
an assumed raster pattern. It preserves the firmware's order of operations so
that scan motion, point counters, file expectations, and boundary behavior can
be checked independently.\textsuperscript{[2,4]}

\vspace{3pt}

\noindent
\begin{minipage}[t]{0.43\linewidth}
  \vspace{0pt}
  \subsubsection{Firmware-to-Model Traceability}

  The image command clears the counters, selects the forward direction,
  restores the initial \(x,y\) values, opens forward, backward, and parameter
  files, and starts Timer~2 (lines 838--857). Timer~2 then commands the current
  \(x\) position, updates the next \(x\), stores the feedback output, changes
  direction, steps \(y\), and advances the line number (lines
  1178--1219).\textsuperscript{[2]}

  Because the DAC receives the current \(x\) before \(x\) is updated, an
  \(N\)-point line samples \(x_0\) through \(x_0+(N-1)\Delta x\) forward and
  \(x_0+N\Delta x\) through \(x_0+\Delta x\) backward. The companion animation
  reproduces this endpoint offset instead of drawing an idealized
  raster.\textsuperscript{[2,4]}

  \subsubsection{Intended Binary File Contract}

  The forward and backward arrays are declared as signed
  \texttt{int16\_t} values (lines 155--157), and each file write explicitly
  writes two bytes (lines 1188--1198). A valid completed pass is therefore
  intended to contain \(N_xN_y\) values, or \(2N_xN_y\) bytes, in each
  direction file. The parameter file records \(N_x\), \(N_y\), completion
  counters, bias, and current (lines 1248--1262).\textsuperscript{[2]}

  \subsubsection{Unresolved Backward-Index Defect}

  The arrays are only 512 points wide (lines 105--108 and 155--157), but the
  backward pass uses raw \texttt{pointcounter} values from \(N_x\) through
  \(2N_x-1\) as its second array index (lines 1194--1203). At the default
  \(N_x=512\), the first backward index is already outside the array. The
  Python model flags this defect; it does not claim the firmware is
  corrected.\textsuperscript{[2,4]}
\end{minipage}
\hfill
\begin{minipage}[t]{0.54\linewidth}
  \vspace{0pt}
  \centering
  \includegraphics[width=\linewidth]{scan-pattern-sequence.png}
  \captionsetup{font=scriptsize}
  \captionof{figure}{Four verified states of the literal Timer~2 scan order.
  Filled points show forward samples, open points show backward samples, and
  the diamond marks the commanded tip position.\textsuperscript{[2,4]}}
\end{minipage}

\vspace{4pt}
\subsubsection{Verification Matrix}

\begin{center}
\scriptsize
\renewcommand{\arraystretch}{1.08}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.22\linewidth}
                >{\raggedright\arraybackslash}p{0.25\linewidth}
                >{\raggedright\arraybackslash}p{0.36\linewidth}
                p{0.10\linewidth}@{}}
  \toprule
  Behavior & Source evidence & Independent check & Status \\
  \midrule
  Scan initialization & Lines 838--857 & Counters, direction, files, and
    initial coordinates represented & Matched \\
  Forward/backward order & Lines 1183--1203 & Literal DAC-before-update order
    and \(2N_x\) ticks per line & Matched \\
  Line transition & Lines 1203--1219 & Reset \(x\), increment \(y\), trigger,
    and advance line & Matched \\
  Completion and metadata & Lines 1225--1262 & Stop and parameter fields
    traced directly; not simulated & Source-traced \\
  Two-byte sample width & Lines 1188--1198 & Declarations and write length
    agree; adjacent comment conflicts & Conflict noted \\
  Backward array bounds & Lines 105--108, 155--157, 1194--1203 &
    Default first backward index \(=512\) & Unresolved \\
  \bottomrule
\end{tabular}
\end{center}

\end{spacing}

\clearpage

% -------------------------------------------------
% CENTER ROW 2, SOFTWARE COLUMN
% SHEET 7 OF 12 -- STM CONTROL LOGIC AND SCAN EXECUTION
% -------------------------------------------------
\section{STM Control Logic and Scan Execution}

% This page is about what the code does step by step, not how every file and
% function is arranged. Sheet 5 already handles the overall organization.
\begingroup
% This sheet has more code than the others, so keep its spacing a little tighter.
\titlespacing*{\subsubsection}{0pt}{0.42cm}{1.5pt}
\setlength{\abovedisplayskip}{3pt}
\setlength{\belowdisplayskip}{3pt}
\begin{spacing}{0.94}
\footnotesize

The firmware converts an STM measurement into a repeatable operating sequence:
approach the sample, confirm a tunneling signal, activate constant-current
feedback, execute the raster scan, and close the data files safely. Separate
state flags and timer interrupts ensure that each process runs only after its
required conditions have been satisfied.\textsuperscript{[2,3]}

\vspace{2pt}

\noindent
\begin{minipage}[t]{0.48\linewidth}
  \vspace{0pt}

  \subsubsection{Automated Approach and Signal Confirmation}

  Feedback is disabled during approach. The program gradually changes the
  vertical DAC output and reads the ADC after each change. When the signal
  exceeds the programmed positive or negative threshold, the ADC is read a
  second time. Only two consecutive threshold crossings are accepted as a
  tunneling signal. If no signal is confirmed during the fine sweep, the
  vertical output retracts, the coarse motor advances one step, and the
  routine tries again.\textsuperscript{[2]}

  \noindent{\tiny\textit{Code:}
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1119--1159.\textsuperscript{[2]}\par}

  \subsubsection{Timer 1: Constant-Current Feedback}

  % This is simplified PID math so visitors can understand the idea without
  % reading the full library. Insert our final Kp, Ki, and Kd here for future.
  After confirmation, Timer~1 executes every \(100\,\mu\mathrm{s}\). It reads
  the ADC, calculates a new PID output, and writes that correction to DAC
  channel~0.\textsuperscript{[2]} The supplied PID routine uses
  \[
    u_n=K_Pe_n+I_n-K_D(x_n-x_{n-1}),
  \]
  where \(e_n\) is the setpoint error and \(x_n-x_{n-1}\) is the recent change
  in measured input. The accumulated integral term and final output are
  clamped to the configured limits to prevent requests outside the DAC
  range.\textsuperscript{[3]}

  \noindent{\tiny\textit{Code:}
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1164--1175;\enspace \nolinkurl{PID_vInt.cpp}, lines
  48--78 and 134--148.\textsuperscript{[2,3]}\par}

  \subsubsection{Timer 2: Raster-Scan Execution}

  Timer~2 writes the current \(x\) command to the DAC, updates \(x\) for the
  next timer tick, and stores the current feedback output. After the forward
  line reaches the selected number of points, the direction reverses and a
  backward line is collected. Completing both passes resets \(x\), increments
  \(y\), and starts the next line. When the selected number of lines is
  reached, the timer stops, writes the parameters, and closes all
  files.\textsuperscript{[2]}

  \noindent{\tiny\textit{Code:}
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1178--1239.\textsuperscript{[2]}\par}

  \vspace{3pt}

  \noindent\textbf{Equation Variable Legend}

  \vspace{2pt}

  \noindent
  {\tiny
  \renewcommand{\arraystretch}{1.0}
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.10\linewidth}
                  >{\raggedright\arraybackslash}p{0.35\linewidth}
                  >{\raggedright\arraybackslash}p{0.10\linewidth}
                  >{\raggedright\arraybackslash}p{0.35\linewidth}@{}}
    \toprule
    \textbf{Var.} & \textbf{Meaning} & \textbf{Var.} & \textbf{Meaning} \\
    \midrule
    \(u_n\) & PID output at update \(n\) & \(K_P\) & Proportional gain \\
    \(e_n\) & Setpoint error & \(I_n\) & Integral correction \\
    \(K_D\) & Derivative gain & \(x_n\) & Current ADC input \\
    \(x_{n-1}\) & Previous ADC input & \(n\) & Update number \\
    \bottomrule
  \end{tabular}
  }

  \vspace{3pt}

  \noindent\textbf{Timer 1 Source Excerpt}

  \begin{lstlisting}[
    language=C++,
    basicstyle=\ttfamily\tiny,
    numbers=left,
    numberstyle=\tiny,
    numbersep=4pt,
    firstnumber=1168,
    aboveskip=2pt,
    belowskip=1pt
  ]
Input = ADC_Convert();
myPID.Compute();
int Value = Output;
SetDAC(0, Value);
  \end{lstlisting}
  \noindent{\tiny Timer~1 reads the ADC, runs the PID, and updates DAC~0.
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1168--1171.\textsuperscript{[2]}\par}
\end{minipage}
\hfill
\begin{minipage}[t]{0.49\linewidth}
  \vspace{0pt}
  \centering
  % Keep this diagram simple because the pseudocode right below it gives the
  % extra detail. Update both if the retry path or timer order changes.
  \includegraphics[width=0.88\linewidth]{software-state-sequence.png}
  \captionsetup{font=scriptsize}
  \captionof{figure}{Operating states and the no-signal retry
  path.\textsuperscript{[2]}}

  \raggedright
  \subsubsection{Operating Sequence in Pseudocode}

  % This is not copied code. It is just the logic rewritten so people can read
  % it quickly. Insert any final shutdown or error-handling step here for future.
  The pseudocode summarizes the conditions that move the controller from one
  state to the next.

  \begin{lstlisting}[language={},basicstyle=\ttfamily\tiny]
INITIALIZE
  set interfaces and safe outputs
APPROACH
  sweep Z; read current
  confirmed twice -> start Timer 1
  no signal -> retract; motor step; retry
TIMER 1 -- every 100 us
  current <- ADC
  Z DAC <- limited PID correction
TIMER 2 -- each point
  step X; store correction
  forward done -> reverse X
  backward done -> reset X; step Y
  final line -> save; close files
  \end{lstlisting}

  \subsubsection{Raster State Update}

  \begin{lstlisting}[
    language=C++,
    basicstyle=\ttfamily\tiny,
    numbers=left,
    numberstyle=\tiny,
    numbersep=4pt,
    firstnumber=1201,
    aboveskip=2pt,
    belowskip=1pt
  ]
pointcounter++;
if (pointcounter >= numpoints) fscan = false;
if (pointcounter >= (2 * numpoints)) {
  fscan = true;
  pointcounter = 0;
  if (yscan) {
    yv += stepsize;
    SetDAC(2, yv);
  }
  if (xscan) xv = xvinit;
  \end{lstlisting}
  \noindent{\tiny After both \(x\) passes, the code resets the point state,
  increments \(y\), and returns \(x\) to its start.
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1201--1210.\textsuperscript{[2]}\par}

\end{minipage}

\end{spacing}
\endgroup

\clearpage

% -------------------------------------------------
% CENTER ROW 3, HARDWARE COLUMN
% SHEET 8 OF 12 -- SUBSYSTEM TESTING AND CALIBRATION
% -------------------------------------------------
\section{Subsystem Testing and Calibration}

\begin{spacing}{1.0}
\small

The procedures required to validate the hardware are defined below, but the
project archive does not yet contain the corresponding readings, instrument
settings, or screenshots. No subsystem is assigned a pass/fail result until
those measurements are recorded.

\vspace{6pt}

\begin{center}
\footnotesize
\renewcommand{\arraystretch}{1.20}
\setlength{\tabcolsep}{2pt}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.14\linewidth}
                >{\raggedright\arraybackslash}p{0.25\linewidth}
                >{\raggedright\arraybackslash}p{0.19\linewidth}
                >{\raggedright\arraybackslash}p{0.16\linewidth}
                >{\raggedright\arraybackslash}p{0.12\linewidth}
                p{0.08\linewidth}@{}}
  \toprule
  Subsystem & Procedure & Expected value or behavior & Measured & Difference
    & Status \\
  \midrule
  Positive rail & Multimeter from \(+V\) to ground & \(+15\) V target &
    Not recorded & --- & Pending \\
  Negative rail & Multimeter from \(-V\) to ground & \(-15\) V target &
    Not recorded & --- & Pending \\
  Analog chain & Known function-generator input; record input and output
    scope channels & Gain and phase predicted from the tested circuit &
    Not recorded & --- & Pending \\
  ADS8691 ADC & Apply known voltage; compare count conversion with multimeter &
    Converted voltage agrees with applied voltage & Not recorded & --- &
    Pending \\
  DAC8734 & Command fixed values or waveform; measure output on oscilloscope &
    Output corresponds to requested code and waveform & Not recorded & --- &
    Pending \\
  \bottomrule
\end{tabular}
\end{center}

\subsection{Required Test Record}

For every row, the final record must include the date, wiring configuration,
instrument model, range or volts-per-division, commanded or applied value,
measured value with units, calculated difference, and an explicit acceptance
criterion. Oscilloscope captures and calibration plots belong on the
supplementary-evidence sheet.

\end{spacing}

\clearpage

% -------------------------------------------------
% CENTER ROW 3, SOFTWARE COLUMN
% SHEET 9 OF 12 -- DATA OUTPUT AND SURFACE RECONSTRUCTION
% -------------------------------------------------
\section{Data Output and Surface Reconstruction}

% Write this page like the reader is following the finished workflow. Leave the
% real file names, settings, and results blank until we actually have them.
\begin{spacing}{1.0}
\footnotesize

The controller produces a sequence of vertical feedback corrections as the tip
moves across the sample. The reconstruction program reads the forward scan,
backward scan, and parameter files; verifies their dimensions; reshapes the
values into an \(x,y\) matrix; and converts the result into a surface map.

\vspace{5pt}

\noindent
{\centering
  % This is the overview image. If our real code skips or adds a processing
  % step, update the PNG so the picture still matches the paragraphs.
  \includegraphics[width=0.90\linewidth]{data-output-surface-reconstruction.png}
  \captionsetup{font=footnotesize}
  \captionof{figure}{Processing workflow from the firmware-defined scan files
  to a reconstructed surface map.\textsuperscript{[2]}}
  \par
}

\vspace{4pt}

\noindent
\begin{minipage}[t]{0.47\linewidth}
  \vspace{0pt}

  \subsubsection{Interpreting the Data Output}

  In constant-current operation, the feedback loop continually changes the
  vertical control signal to keep tunneling current near its setpoint. The
  firmware stores this PID correction as a signed 16-bit value and writes the
  forward and backward passes to separate files.\textsuperscript{[2]} These
  numbers are controller corrections rather than photographs or automatically
  calibrated surface heights.

  The reconstruction program also reads the parameter file, which records the
  requested number of points \(N_x\), number of lines \(N_y\), bias, and
  current setting.\textsuperscript{[2]} It checks that each scan contains
  \(N_xN_y\) stored values before beginning reconstruction. A different file
  length is identified as an incomplete or inconsistent scan rather than being
  silently filled.

  \noindent{\scriptsize\textit{Code reference:}
  \nolinkurl{Teensy_STM11A_July_29_2025.ino}, lines
  1188--1198 and 1248--1262.\textsuperscript{[2]}\par}

  \subsubsection{From a Sequence to a Surface}

  % This formula only explains how a flat file becomes rows and columns.
  % Insert the actual Nx and Ny in the table on the right, not in this formula.
  If \(q_k\) is the \(k\)-th stored correction, the program arranges the
  one-dimensional sequence into a two-dimensional matrix:
  \[
    Q_{j,i}=q_{jN_x+i},
    \qquad
    0\leq i<N_x,\quad 0\leq j<N_y .
  \]
  Each matrix column represents an \(x\)-position and each row represents a
  scan line. The program reverses backward lines into the same spatial
  orientation as the forward lines before making a point-by-point comparison.
\end{minipage}
\hfill
\begin{minipage}[t]{0.50\linewidth}
  \vspace{0pt}

  \subsubsection{Calibration, Comparison, and Visualization}

  % Alpha stays symbolic until we measure it. Do not put nm/count here just
  % because it looks right; insert the measured value in the table later.
  Before calibration, \(Q\) is plotted as relative vertical correction in DAC
  or PID counts. A measured vertical-piezo calibration factor \(\alpha\),
  expressed in distance per count, converts the relative values into height:
  \[
    z_{j,i}=\alpha\left(Q_{j,i}-Q_{\mathrm{ref}}\right).
  \]
  The reference \(Q_{\mathrm{ref}}\) sets the displayed zero level without
  changing the measured height differences. The script displays the forward
  and backward maps separately before combining them because disagreement can
  reveal drift, feedback lag, vibration, or an incorrect line orientation.

  % Insert the final Matplotlib settings here for future, especially any
  % leveling, filtering, interpolation, or color limits that change the image.
  The 2D reconstruction assigns position to the horizontal axes and relative
  correction or calibrated height to color. The 3D view uses the same matrix
  as its vertical coordinate. The program retains the unprocessed matrix before
  applying plane subtraction, line leveling, or filtering, allowing processed
  features to be checked against the original values.

  \subsubsection{Scan and Reconstruction Record}

  % DATA TABLE:
  % Fill this in once we have a complete scan and the code is working.
  % Make sure every physical value has its units.
  \begin{center}
  \renewcommand{\arraystretch}{1.18}
  \begin{tabular}{@{}p{0.46\linewidth}p{0.47\linewidth}@{}}
    \toprule
    \textbf{Record} & \textbf{Value} \\
    \midrule
    Forward-scan file & \rule{0.92\linewidth}{0.4pt} \\
    Backward-scan file & \rule{0.92\linewidth}{0.4pt} \\
    Parameter file & \rule{0.92\linewidth}{0.4pt} \\
    Matrix size, \(N_x\times N_y\) & \rule{0.92\linewidth}{0.4pt} \\
    Bias and current setpoint & \rule{0.92\linewidth}{0.4pt} \\
    Vertical calibration, \(\alpha\) & \rule{0.92\linewidth}{0.4pt} \\
    Processing code/version & \rule{0.92\linewidth}{0.4pt} \\
    \bottomrule
  \end{tabular}
  \end{center}

  % Once we have the data, replace this with how we ran the scan, whether it
  % finished, and anything weird that happened while collecting it.
  \textit{Add about the collected data and scan conditions.}

  % Once the code works, replace this with what it checks, what processing we
  % used, and where the script is in clustered.
  \textit{Add about the reconstruction code and processing choices.}

  % Once we have the final maps, replace this with how the forward and backward
  % scans compare and what we can actually see in the surface.
  \textit{Add about the reconstructed surface and scan agreement.}
\end{minipage}

\end{spacing}

\end{landscape}

% =================================================
% RIGHT PANEL: THREE PORTRAIT SHEETS
% =================================================

% -------------------------------------------------
% SHEET 10 OF 12 -- RESULTS AND UNCERTAINTY ANALYSIS
% -------------------------------------------------
\section{Results and Uncertainty Analysis}

\begin{spacing}{1.0}
\small

\subsection{Key Findings}

\begin{center}
\footnotesize
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.35\textwidth}
                >{\raggedright\arraybackslash}p{0.46\textwidth}
                p{0.12\textwidth}@{}}
  \toprule
  Finding & Evidence & Status \\
  \midrule
  Firmware scan order is traceable & Teensy lines 838--857 and 1178--1239;
    independently reproduced by the Python animation & Verified \\
  Executable storage path is signed 16-bit & Array declarations and explicit
    two-byte writes at lines 155--157 and 1188--1198; adjacent comment
    conflicts & Code traced \\
  Default backward storage is unsafe & Raw backward indices begin at
    \(N_x=512\) for a 512-wide array & Unresolved \\
  Quantitative subsystem performance & No complete readings, settings, or
    acceptance criteria are stored in the project archive & Pending \\
  Calibrated surface imaging & No complete scan files or measured
    vertical-piezo calibration are available & Pending \\
  \bottomrule
\end{tabular}
\end{center}

\subsection{Sources of Uncertainty}

\noindent
\begin{minipage}[t]{0.47\textwidth}
  \subsubsection{Demonstrated Software Risks}

  The backward-array index can address memory outside the declared row. The
  source also describes a stored value as ``18 bit'' and ``4 bytes'' while
  declaring \texttt{int16\_t} arrays and writing two bytes. These
  inconsistencies must be resolved before backward files are treated as valid
  measurements.\textsuperscript{[2]}
\end{minipage}
\hfill
\begin{minipage}[t]{0.47\textwidth}
  \subsubsection{Experimental Factors to Quantify}

  Vibration, electrical noise, thermal drift, piezo hysteresis and creep, tip
  condition, feedback tuning, and scan speed may affect an STM measurement.
  The available records do not yet show which factor dominates, so none is
  presented as an observed limitation or assigned a numerical uncertainty.
\end{minipage}

\subsection{Recommended Improvements}

\begin{enumerate}
  \item Correct the backward index to use a zero-based column and test the
  first, direction-change, and final indices at \(N_x=1\) and \(N_x=512\).
  \item Define one binary file schema with sample width, signedness,
  dimensions, byte order, firmware version, and completion status.
  \item Record the hardware table's measurements and acceptance criteria
  before restoring any claim of confirmed subsystem performance.
  \item Acquire complete forward, backward, and parameter files; verify their
  lengths; compare the two scan directions; and retain the raw matrices.
  \item Measure the vertical-piezo calibration before converting correction
  counts into physical height.
\end{enumerate}

\end{spacing}

\clearpage

% -------------------------------------------------
% SHEET 11 OF 12 -- SUPPLEMENTARY FIGURES
% -------------------------------------------------
\section{Supplementary Figures}

\subsection{Reserved Experimental Evidence}

This sheet is intentionally reserved for measured outputs rather than
synthetic or decorative figures. Each item below remains pending until its
source values, axes, units, and acquisition settings are available.

\begin{center}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.30\textwidth}
                >{\raggedright\arraybackslash}p{0.52\textwidth}
                p{0.11\textwidth}@{}}
  \toprule
  Planned figure & Required content & Status \\
  \midrule
  Power-rail verification & Positive and negative readings, meter range,
    ground reference, and difference from target & Pending \\
  Analog signal-chain capture & Input and output waveforms with frequency,
    amplitude, coupling, and volts-per-division & Pending \\
  ADC calibration curve & Applied voltage, ADC counts, converted voltage,
    residuals, and fitted relationship & Pending \\
  DAC response & Requested code or waveform compared with measured output &
    Pending \\
  Direction comparison & Forward and spatially reversed backward matrices
    plotted on identical scales & Pending \\
  Surface reconstruction & Final 2D map with raw-data path, processing
    version, units, and calibration status & Pending \\
  \bottomrule
\end{tabular}
\end{center}

\vspace{1cm}
\noindent
No placeholder graph is inserted because doing so could be mistaken for an
instrument result. The scan-order animation has moved to the software
verification sheet, where it is explicitly identified as a firmware model.

\clearpage

% -------------------------------------------------
% SHEET 12 OF 12 -- REFERENCES AND ACKNOWLEDGMENTS
% -------------------------------------------------
\section{References and Acknowledgments}

% If we add a new source anywhere else, add it here and make sure the little
% citation number in the text matches this list.
\noindent
\begin{minipage}[t]{0.72\textwidth}
  \vspace{0pt}
  \subsection{References}

  \begin{enumerate}
    \item S. Chiang, ``Additional Quantum Mechanics Discussion,''
    \textit{QMnotes1.pdf}, COSMOS course handout, 2026.

    \item S. Chiang, \textit{Teensy STM11A}, STM controller source file
    \nolinkurl{Teensy_STM11A_July_29_2025.ino}, University of California,
    Davis, 2025. Cited portions include lines 105--180, 838--857, and
    1119--1262.

    \item B. Beauregard, \textit{Arduino PID Library}, version 1.1.1,
    timer-interrupt modification by S. Chiang, source file
    \nolinkurl{PID_vInt.cpp}, course software archive. Cited portions include
    lines 48--78 and 134--148.

    \item souuk, \textit{clustered: STM Project Files and Citation-Source
    Paths}, GitHub repository, 2026. Available:
    \href{https://github.com/souuk/clustered}{github.com/souuk/clustered}.
    The repository includes \nolinkurl{animate_scan_pattern.py} and
    \nolinkurl{scan-pattern-preview.gif}; source locations within the supplied
    course archive are listed in the repository's
    \href{https://github.com/souuk/clustered\#citation-path-index}
    {citation path index}.
  \end{enumerate}
\end{minipage}
\hfill
\begin{minipage}[t]{0.23\textwidth}
  \vspace{0pt}
  \raggedleft
  % This makes the QR code straight from the link, so we do not need to upload
  % another PNG for it in Overleaf.
  \href{https://github.com/souuk/clustered}{%
    \qrcode[height=1.25in]{https://github.com/souuk/clustered}%
  }

  \vspace{5pt}
  {\small\bfseries STM source code\par}
  {\footnotesize github.com/souuk/clustered\par}
\end{minipage}

\subsection{Acknowledgments}

We are deeply grateful to the instructors and mentors whose guidance,
encouragement, patience, and generosity made this project possible.

We thank Professor Shirley Chiang for guiding the experimental side of the
project. Her instruction in E\&M, STM operation, and circuitry gave us the
practical foundation needed to construct and test the microscope. We are
especially grateful for the insight she provided throughout the work.

We also thank Professor Richard Scalettar for providing the computational labs
that supported this project. These labs allowed us to model quantum systems
and simulate physical behavior that we could not directly reproduce with our
experimental setups. We also thank him for all the amazing free clothes; they
will decorate our souls for years to come.

Finally, we thank Teaching Intern ``Multimeter Whisperer'' Gibson Meeker.
Although we sometimes joked that his official role was to ``babysit us,'' his
constant advice, patience, and good humor made every stage of the project
smoother and far more enjoyable. His steady presence and, of course, his
invention of the ``Gibson Tech'' were invaluable parts of our COSMOS
experience.

\end{document}
